const getResolution = () => {
    console.log("Width " + screen.width + "Height " + screen.height);
}

export default getResolution;

/**
 * Get the resolution
 * Display the number of components based on resolution
 */